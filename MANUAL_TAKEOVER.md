# 施工区人工接管

## 工作流程

1. 第二圈配置启用 `busy`、`manualTakeover` 后，车辆识别到施工区标志。
2. 车端连续鸣警报并将速度置零，保持手动接管状态。
3. 操作员只看电脑端第一视角画面，用键盘驶过施工路况。
4. 通过障碍、摆正车辆后，在到达第一个停靠框之前按 `R`，确认后切回自动模式。
5. 自动模式重新接管后，从第一个停靠框开始真实计数，并在配置的目标框停车。

远程客户端连接本身不应被当作施工区触发条件；施工区识别结果才是接管触发条件。

## 电脑端启动

电脑与小车接入同一网络，安装依赖并运行：

```bash
python -m pip install -r tools/requirements-manual.txt
python tools/manual_control_client.py <小车IP>
```

车端默认监听 TCP `8080` 端口。

| 按键 | 功能 |
|---|---|
| `Shift`（持续按住） | 遥控使能；松开立即停止发送运动命令 |
| `W` / `S` | 前进 / 后退（须同时按住 Shift） |
| `A` / `D` | 左转 / 右转，可与前进后退组合（须同时按住 Shift） |
| `空格` | 立即停车 |
| `R` | 确认后切回自动模式 |

客户端以 10 Hz 发送控制/心跳。画面上的绿色透视引导线用于估计车身通道，
黄色横线辅助判断距离。关闭窗口前客户端会先发送停车命令。
窗口失去焦点会立即清空按键并发送锁存急停；后台线程还会监控 GUI 主线程
心跳，超过 300 ms 未刷新就清空按键并急停。长按运动键不依赖操作系统的
键盘自动重复，因此不会因首次重复延迟而断续。

## 上车前检查

- 架空驱动轮，逐一检查前进、后退和左右方向。
- 确认施工区被识别后车辆确实停止并鸣警报。
- 拔掉电脑网线或关闭 Wi-Fi，确认通信丢失后车辆保持停止，不能继续沿用最后指令。
- 通过障碍且车头与自动循迹方向基本一致后、到达第一个停靠框前按 `R`。
- 实车首次测试使用最低可控速度，并安排独立急停人员。
